/** Tab activo en controles segmentados del compositor (relleno sólido). */
export const COMPOSITOR_SEGMENTED_TAB_ACTIVE_CLASS =
  "bg-compositor-config text-white shadow-sm";

/**
 * Botón de acción del compositor: relleno visible + borde, legible y distinto del tab activo.
 */
export const COMPOSITOR_ACTION_BUTTON_CLASS =
  "border border-compositor-config/50 bg-[color-mix(in_srgb,var(--compositor-config)_32%,var(--bg-card))] font-bold text-white";

/** Agregar bloque: mismo verde del módulo, fondo más clarito (no CTA oscuro). */
export const COMPOSITOR_ADD_BLOCK_BUTTON_CLASS =
  "border border-compositor-config/35 bg-[color-mix(in_srgb,var(--compositor-config)_22%,white_65%)] font-bold text-compositor-config";
